/*!
 * Initialisations & customisations
 */

document.addEventListener('DOMContentLoaded', function() {

  /* Init Sidenav (shorthand) */

  // M.Sidenav.init(document.querySelectorAll('.sidenav'));

  /* Init Sidenav (shorthand with options) */

  // M.Sidenav.init(document.querySelectorAll('.sidenav'),{
  //   edge: 'right'
  // });

  /* Init Sidenav (as per documentation) */

  // var elems = document.querySelectorAll('.sidenav');
  // var instances = M.Sidenav.init(elems);

  /* Init Sidenav (as per documentation with options) */
  
  // var elems = document.querySelectorAll('.sidenav');
  // var instances = M.Sidenav.init(elems,{
  //   edge: 'right'
  // });

});

